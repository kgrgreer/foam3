package net.nanopay.auth.password;

import foam.core.X;
import foam.nanos.http.WebAgent;

public class ResetPasswordWebAgent
    implements WebAgent
{
  @Override
  public void execute(X x) {
    // TODO: redirect to reset password screen
  }
}
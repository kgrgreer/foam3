p({
  "class":"foam.nanos.boot.NSpec",
  "name":"doNotSolicitDAO",
  "serve":true,
  "serviceScript":"""
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(net.nanopay.auth.email.DoNotSolicit.getOwnClassInfo())
      .setGuid(true)
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .setJournalName("doNotSolicit")
      .build();
  """,
  "client":"""
    {
      "of":"net.nanopay.auth.email.DoNotSolicit"
    }
  """
})

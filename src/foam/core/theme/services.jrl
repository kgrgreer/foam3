p({
  "class": "foam.core.boot.CSpec",
  "name": "themeDAO",
  "description": "Stores Themes, which are used to style the application.",
  "serve": true,
  "authenticate": false,
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(foam.core.theme.Theme.getOwnClassInfo())
      .setGuid(true)
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .setJournalName("themes")
      .setDecorator(new foam.core.theme.ThemeDomainsDAO.Builder(x).setDelegate(new foam.dao.NullDAO(x, foam.core.theme.Theme.getOwnClassInfo())).build())
      .setPm(true)
      .setServiceProviderAware(false)
      .setPermissioned(true)
      .build()
      .addPropertyIndex(new foam.lang.PropertyInfo[] {foam.core.theme.Theme.NAME, foam.core.auth.GroupPermissionJunction.TARGET_ID});
  """,
  "client": "{\"of\":\"foam.core.theme.Theme\"}",
  "authNotes": "Needs to be unauthenticated because we need to be able to theme apps before the user has logged in. However, we still need to restrict write access, which we do via an AuthorizationDAO."
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "themeDomainCapabilityJunctionDAO",
  "serve": true,
  "authenticate": false,
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(foam.core.theme.ThemeDomainCapabilityJunction.getOwnClassInfo())
      .setGuid(true)
      .setAuthorizer(new foam.core.auth.GlobalReadAuthorizer("themedomaincapabilityjunction"))
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .setJournalName("themeDomainCapabilityJunctions")
      .setPm(true)
      .build()
  """,
  "client": "{\"of\":\"foam.core.theme.ThemeDomainCapabilityJunction\"}",
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "spThemeDAO",
  "description": "Access to Themes specific to Service Provider",
  "serve": true,
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setPm(false)
      .setOf(foam.core.theme.Theme.getOwnClassInfo())
      .setInnerDAO(x.get("themeDAO"))
      .setServiceProviderAware(true)
      .build();
  """,
  "client": "{\"of\":\"foam.core.theme.Theme\"}",
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "themeDomainDAO",
  "description": "Provides domain lookup for Themes",
  "serve": true,
  "authenticate": false,
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(foam.core.theme.ThemeDomain.getOwnClassInfo())
      .setAuthorizer(new foam.core.auth.GlobalReadAuthorizer("themedomain"))
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .setJournalName("themeDomains")
      .setPm(true)
      .build()
  """,
  "client": "{\"of\":\"foam.core.theme.ThemeDomain\"}",
  "authNotes": "Needs to be unauthenticated because we need to be able to theme apps before the user has logged in. However, we still need to restrict write access, which we do via an AuthorizationDAO."
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "themes",
  "serve": true,
  "authenticate":false,
  "lazy": false,
  "lazyOrder": -1,
  "lazyClient": false,
  "boxClass":"foam.core.theme.ThemeServiceSkeleton",
  "serviceClass": "foam.core.theme.Themes",
  "client": """
    { 
      "class": "foam.core.theme.ContextRegistrationThemeService",
      "delegate": { 
        "class": "foam.core.theme.ClientThemeService",
        "delegate": {
          "class":"foam.box.SessionClientBox",
          "delegate": {
            "class":"foam.box.HTTPBox",
            "url": "service/themes"
          }
        }
      }
    }
  """
})

p({
  "class":"foam.core.boot.CSpec",
  "name":"cssTokenOverrideService",
  "serve":true,
  "lazyClient": false,
  "authenticate":false,
  "client":"{ \"class\": \"foam.core.theme.customisation.CSSTokenOverrideService\" }"
})

p({
  "class":"foam.core.boot.CSpec",
  "name":"cssTokenOverrideDAO",
  "description":"Theme CSS Token Override DAO",
  "serve":true,
  "authenticate":false,
  "serviceScript":
  """
    return new foam.dao.EasyDAO.Builder(x)
      .setPm(false)
      .setAuthorizer(new foam.core.auth.GlobalReadAuthorizer("csstokenoverride"))
      .setJournalName("cssTokenOverrides")
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .setOf(foam.core.theme.customisation.CSSTokenOverride.getOwnClassInfo())
      .build()
  """,
  "client":
  """
    {
      "of":"foam.core.theme.customisation.CSSTokenOverride"
    }
  """
})

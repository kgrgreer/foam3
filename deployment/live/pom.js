foam.POM({
  name: 'live',
  help: 'Live reload for source runs: watches the .js files under the webroot and pushes each change to open browsers, which reload the changed class in place (foam.u2.ViewReloader). Added by ./build.sh -l.'
});
